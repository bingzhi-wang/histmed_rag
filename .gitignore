# Python
venv/
__pycache__/
*.pyc

# Models — large; users download these via the install steps, don't commit
models/

# Runtime outputs — generated per run
logs/
exports/
test_runs/
*.log

# Docker data volumes (Qdrant / Elasticsearch indexes)
docker/qdrant_data/
docker/es_data/

# OS / editor
.DS_Store
.idea/
.vscode/

# Corpus raw sources — uncomment if large or license-restricted.
# Keep corpus/processed/*.jsonl committed so others can index without
# re-downloading and re-translating.
# corpus/raw/
